/* Copy this file to config.js (same folder) and fill in your real values from
   Supabase → Settings → API. Then never touch it again — future updates to
   index.html won't overwrite it, since it's a separate file. */
window.PRACTEX_CONFIG = {
  SUPABASE_URL: 'YOUR_SUPABASE_PROJECT_URL',       // e.g. https://abcdefghijk.supabase.co
  SUPABASE_ANON_KEY: 'YOUR_SUPABASE_ANON_KEY'       // Settings → API → Project API keys → anon public
};
